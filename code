//calculate number is even or odd//
#include<stdio.h>
int main()
{
int number;
scanf("%d",&number);
if(number%2==0)
printf("%d is an even number",number);
else
printf("%d ia an odd number",number);
return 0;
}
